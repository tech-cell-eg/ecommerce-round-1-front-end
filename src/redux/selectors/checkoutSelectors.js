export const selectActiveStep = (state) => state.checkout.activeStep;
export const selectSelectedAddress = (state) => state.checkout.selectedAddress;
export const selectSelectedPayment = (state) => state.checkout.selectedPayment;
export const selectAddresses = (state) => state.checkout.addresses;
export const selectOrderConfirmed = (state) => state.checkout.orderConfirmed;
export const selectDiscount = (state) => state.checkout.discount;
export const selectDeliveryCharge = (state) => state.checkout.deliveryCharge;
